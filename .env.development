# Development Environment Variables
# These are loaded automatically when running `npm run dev`

# API Configuration
# When using the proxy (recommended), use relative paths
# When not using proxy, set to full URL: http://localhost:4000
VITE_API_URL=

# Feature flags (optional)
VITE_ENABLE_DEV_TOOLS=true
